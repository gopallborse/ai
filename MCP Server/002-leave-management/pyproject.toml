[project]
name = "mcp-leave-manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gopall Borse", email = "gopallborse@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=2.1.1",
]

[project.scripts]
mcp-leave-manager = "mcp_leave_manager:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
